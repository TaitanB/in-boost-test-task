export const selectNodes = state => state?.node?.nodes;
export const selectEdges = state => state?.node?.edges;
export const selectChoosedVariants = state => state?.node?.choosedVariants;
export const selectSelectedText = state => state?.node?.selectText;
